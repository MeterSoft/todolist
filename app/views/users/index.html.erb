<% @users.each_with_index do |user, i| %>
    <%= i+1 %>
    <a href="/users/<%=user.id %>" > 
    <%= user.full_name %> 
    <%= link_to 'Delete', { :action => :destroy, :id => user.id }, :method => :delete, :confirm => 'Are you sure?' %>
  </br>
<% end %>
    <a href="/users/new">Add new user</a>



